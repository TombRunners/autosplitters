namespace TR3;

/// <summary>The game's level and demo values.</summary>
internal enum TlaLevel : uint
{
    LarasHome = 00,

    // Scotland
    HighlandFling = 01,
    WillardsLair  = 02,

    // Channel Tunnel
    ShakespeareCliff      = 03,
    SleepingWithTheFishes = 04,

    // Paris
    ItsAMadhouse = 05,
    Reunion      = 06,
}